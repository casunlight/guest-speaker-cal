<%= form_for(@speaker) do |f| %>
  <% if @speaker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@speaker.errors.count, "error") %> prohibited this speaker from being saved:</h2>

      <ul>
      <% @speaker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br />
    <%= f.text_field :full_name %>
  </div>
  <div class="field">
    <%= f.label :lecture_title %><br />
    <%= f.text_field :lecture_title %>
  </div>
  <div class="field">
    <%= f.label :lecture_description %><br />
    <%= f.text_area :lecture_description %>
  </div>
    <input type="hidden" name="speaker[day_id]" value="<%= @day_id %>">
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>